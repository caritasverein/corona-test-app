FROM node:14

ARG FONTAWESOME_KEY_VAR

RUN mkdir /app && chown 1000:1000 /app
WORKDIR /app
USER 1000

ENV PUBLIC_URL=/admin
ENV WDS_SOCKET_PATH=/admin/sockjs-node
ENV FONTAWESOME_KEY=$FONTAWESOME_KEY_VAR

CMD npm i && npm start
